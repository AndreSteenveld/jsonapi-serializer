import Promise from "bluebird";
import defaults from "./defaults";

export function parse_payload( context, request, response ){
    
    return Promise.reject( null );
    
}

export default defaults :: parse_payload;